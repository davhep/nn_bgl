#include <nn_bgl/nn_bgl.h>
#include <iostream>

int main() {
    std::cout << "Neural Network with Boost Graph Library Example" << std::endl;
    std::cout << "=============================================" << std::endl;
    
    // This is a basic example showing the project structure
    // In a real application, you would:
    // 1. Create a neural network
    // 2. Train it with data
    // 3. Use it for predictions
    
    std::cout << "Project successfully refactored to standard C++ structure!" << std::endl;
    std::cout << "Headers are now in include/nn_bgl/" << std::endl;
    std::cout << "Source files are in src/" << std::endl;
    std::cout << "Build with: mkdir build && cd build && cmake .. && make" << std::endl;
    
    return 0;
}
